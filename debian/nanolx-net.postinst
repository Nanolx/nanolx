#!/bin/bash

set -e

if [[ ! -f /etc/privoxy/config.default ]]; then
	mv /etc/privoxy/config /etc/privoxy/config.default
	ln -fs /etc/privoxy/config.default /etc/privoxy/config
fi

#DEBHELPER#

exit 0
